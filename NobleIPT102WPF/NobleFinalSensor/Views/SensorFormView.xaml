<UserControl x:Class="NobleFinalSensor.Views.SensorFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="24" FontWeight="Bold" Margin="0,0,0,20">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0} Sensor">
                    <Binding Path="IsEditMode" Converter="{StaticResource EditModeConverter}"/>
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>

        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Name:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Sensor Type:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding SensorType, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Location:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" Width="100" Margin="0,0,10,0" Padding="5"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Padding="5"/>
        </StackPanel>
    </Grid>
</UserControl>
